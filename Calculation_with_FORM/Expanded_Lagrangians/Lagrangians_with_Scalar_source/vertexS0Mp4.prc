#procedure vertexS0Mp4
(
      + H2 * (
          + 4*tr(Chi,sext)*B0
          )

       + L8 * (
          + 8*tr(Chi,sext)*B0
          )

       + L6 * (
          + 16*tr(sext)*tr(Chi)*B0
          )
)
#endprocedure
