#procedure vertexS0Mp6
(
       + KK27*F^-2 * (
          + 48*tr(sext)*tr(Chi)^2*B0
          )

       + KK26*F^-2 * (
          + 32*tr(Chi)*tr(Chi,sext)*B0
          + 16*tr(sext)*tr(Chi,Chi)*B0
          )

       + KK25*F^-2 * (
          + 48*tr(Chi,Chi,sext)*B0
          )
)
#endprocedure
